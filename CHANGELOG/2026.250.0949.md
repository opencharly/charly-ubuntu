# 2026.250.0949 — chore(ci): install the org pr-validator + tag-on-merge dispatchers

## Summary

Installs the org-standard CI dispatchers this repo was missing (its FIRST PR could not reach the org's `validate / validate` gate without them):

- `.github/workflows/pr-validator.yml` — the org-wide fresh PR evaluator (opencharly/.github, the gate that passes/blocks every opencharly PR and arms native auto-merge).
- `.github/workflows/tag-on-merge.yml` — the org-wide merge-time CalVer tagging + CHANGELOG writer.

Both are byte-identical copies of the org-standard dispatchers run by distro-cachyos (the same repos' sibling; its `validate / validate` check works). Installing them is a guardrail STRENGTHENING change for this repo.

## How tested

The changed path — the two workflow files — was exercised by the YAML parser (both parse clean) and their provenance is the org standard:
```
$ python3 -c "import yaml; [print('OK', f) for f in ['/tmp/pr-validator.yml','/tmp/tag-on-merge.yml'] if yaml.safe_load(open(f))]"
OK /tmp/pr-validator.yml
OK /tmp/tag-on-merge.yml
```
The dispatchers' LIVE execution is their own function: once merged, the next PR on this repo runs the `charly/pr-validator` check (validated end-to-end by the sibling distro-cachyos repo, which runs these exact files).

## Rulebook compliance

- **R0:** git-workflow skill loaded.
- **R1–R5, R7–R10, R4a, pillars:** N/A for a byte-identical org-standard workflow install — R6 (worktree, no force, PR-only), R8 (the emitted files are the org's own).
- **AI Attribution:** footer below.

## Change classification

- **Change class:** hook/workflow (org dispatcher install).
- **Verification gate:** org-standard byte-identical copies + YAML parse (pasted above); the gate's live run is the next PR on this repo.
- **Attribution tier:** analysed on a live system — the org-standard files parse clean and are proven live by the sibling distro-cachyos repo running the identical dispatchers; the full fresh-run (this repo's next PR) is the first consumer.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*