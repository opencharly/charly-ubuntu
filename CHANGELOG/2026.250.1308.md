# 2026.250.1308 — ci: assert packaged charly systemd units + /etc/charly config in install-test

## Summary

Extend the install-test to assert the charly package's new systemd units + system-wide config (shipped since v2026.250.0854):

- unit files exist: `/usr/lib/systemd/system/charly-mcp.service` and `/usr/lib/systemd/user/charly-mcp.service`
- non-autostarting: `systemctl is-enabled charly-mcp` must report `disabled` (a container without a systemd manager reports the absent-manager error, which is accepted); when `systemctl` is absent the [Install] `WantedBy=` section is asserted plus the absence of any enable marker (`multi-user.target.wants` symlink), so a post-install `systemctl enable` can never slip through
- `/etc/charly/charly.yml` exists and `charly box validate -C /etc/charly` exits 0
- the MCP server starts on demand: the test container runs no systemd, so `charly mcp serve --listen 127.0.0.1:18765` (foreground, `timeout 5`, cwd=`/etc/charly`) must stay up until the timeout (exit 124)

(Install step remains `apt-get install -y charly` — only the assertions after the version check change.)

## How tested

- `bash -n` on the extracted install-test script (the docker `bash -c '...'` body incl. the new block):

```
$ bash -n /tmp/charly-charly-ubuntu.sh && echo SYNTAX-OK
SYNTAX-OK
```

- workflow YAML parses (`python3 yaml.safe_load` → 12 steps, block-present checks all True), one file changed, 23 insertions.
- live probes against the released v2026.250.0854 binary + plugin tarball (the exact assets CI packages) with `CHARLY_PLUGIN_DIR`:
  - `charly mcp --help` → exit 0; `charly mcp serve --help` → exit 0 (default listen 127.0.0.1:18765, loopback -- P3)
  - on-demand start probe: `timeout 6 charly mcp serve --listen 127.0.0.1:18766` (from the packaged-config project dir) → `serving 93 tools on http://127.0.0.1:18766/mcp`, exit **124** (ran until timeout) — the same shape the install-test asserts (port 18765 is free in the CI container).

- The full container install-test could not run live: the published repo packages predate the units (published packages are stale pre-250 builds), and the release-candy packaging (`systemd:`/`config:` sections) only renders once plugin-generate-packages is re-released on sdk >= v0.2026250.650 (its latest release v2026.230.0800 pins sdk v0.2026227.1215). Rendered unit contract verified from sdk/packagekit (v0.2026250.650): system unit carries `[Install] WantedBy=multi-user.target`, user unit `WantedBy=default.target`, plus `disable` presets at `/usr/lib/systemd/{system,user}-preset/50-charly.preset`. These assertions are the gate for the next workflow run against a release that ships the units.

## Rulebook compliance

R0-R10 + R4a + pillars (skills-first, git-workflow, strict-policy, root-cause-analyzer, agents) — one line: PR-only feat/ branch, no direct-main push, no submodule gitlink change, minimal single-file CI change, gate-first (the new assertions fail loudly until the package actually ships the units/config).

## Change classification

- change class: CI workflow — install-test assertions
- verification gate: bash -n syntax gate + released-binary live probes; the full install-test executes in repo CI (workflow_dispatch) against the packaged release
- attribution tier: analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*