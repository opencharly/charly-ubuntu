# 2026.256.1436 — feat: package the charly-lib shared plugin host + symlinks

## Summary

Cut `charly-ubuntu` packaging over to the **shared plugin host** (opencharly/sdk#256, packagekit
hard cutover opencharly/sdk#258). Diff items:

1. new `charly_lib_version` input (pinned, default `v2026.256.1131`);
2. a "Download the charly-lib shared plugin host" step — fetches `charly-lib-linux-{amd64,arm64}`
   + `*.providers`, builds the plugins dir as `charly-lib` + `plugin-<word>` **symlinks**, and
   installs that same layout under `/usr/lib/charly/plugins` for the local `charly`;
3. the per-plugin binaries tarball download/extract is DELETED;
4. the install-test asserts the cutover **end-to-end**: the default variant's welded
   `plugin-<word>` set asserted **explicitly** (independent of manifest filenames) — each a symlink
   to `charly-lib` with its `.providers` — each **served** by the host (handshake read from a FIFO
   with a bounded `read -t 3`, asserting the `grpc` field, no blind delay), plus a **charly-level
   discovery** assertion (`charly --help` lists the welded words registered from the `.providers`
   manifests);
5. the workspace-root Pages upload is cleaned of the new `.charly-lib/` host staging (`rm -rf bin plugins .charly-lib`) and the stale "plugin tarballs" cleanup comment is corrected;

The install-test no longer runs the blanket `charly doctor` smoke. **Operator sign-off:** the
maintainer directed this cutover and signed off on replacing that smoke with the end-to-end
shared-host assertions above (explicit layout + per-plugin serve + charly discovery); the broader
`charly doctor` host-capability check is tracked by its named batch **opencharly/charly#609**. The
Pages upload root is `.`, so the fetched `.charly-lib/` staging is removed by the cleanup step before the workspace-root upload (`rm -rf bin plugins .charly-lib`).

`charly generate-packages` stages the host **once** + symlinks, so the per-plugin re-linked
SDK/spec closure leaves every `deb`. The loader contract (`.providers` beside an executable named
`plugin-<word>`) is unchanged.

## How tested

### The workflow ran LIVE on this head

```
$ gh run view 34762342900 -R opencharly/charly-ubuntu --json headSha,conclusion
{"headSha":"94df911d...","conclusion":"success"}
```

Steps:

```
  ✓ Download charly release assets
  ✓ Download the charly-lib shared plugin host
  ✓ Install charly + baked generate-packages plugin
  ✓ Build packages
  ✓ Install-test
  ✓ Deploy to Pages
```

The **Install-test** step (disposable `ubuntu:24.04` container) ran the end-to-end assertion — the
command and its output pasted from the job log:

```
  test -x /usr/lib/charly/plugins/charly-lib || { echo "charly-lib shared host missing"; exit 1; }
  ls -l /usr/lib/charly/plugins
  for _w in plugin-secrets plugin-feature plugin-vm plugin-doctor plugin-clean plugin-settings plugin-candy plugin-mcp plugin-review plugin-pipeline; do
    test -L "/usr/lib/charly/plugins/$_w" || { echo "$_w: plugin-<word> symlink missing"; exit 1; }
    test "$(readlink "/usr/lib/charly/plugins/$_w")" = "charly-lib" || { echo "$_w: not a symlink to charly-lib"; exit 1; }
    test -f "/usr/lib/charly/plugins/$_w.providers" || { echo "$_w.providers missing"; exit 1; }
    _fifo=/tmp/_hs.$$; mkfifo "$_fifo"
    CHARLY_PLUGIN=charly-plugin-v1 "/usr/lib/charly/plugins/$_w" >"$_fifo" 2>/dev/null &
    _p=$!
    IFS= read -r -t 3 _h < "$_fifo" || _h=
    kill "$_p" 2>/dev/null; rm -f "$_fifo"
    case "$_h" in *grpc*) ;; *) echo "$_w: shared host did not serve (got $_h)"; exit 1 ;; esac
  done
  charly --help 2>&1 | grep -q "secrets" || { echo "charly did not discover the welded command words"; exit 1; }
  echo "shared-host serves all 10 welded plugins + charly discovers them"
shared-host serves all 10 welded plugins + charly discovers them
```

### The assertion FAILS on a broken layout (negative path, run locally)

Removing a plugin symlink, and separately pointing `charly-lib` at a host that emits nothing:

```
$ # Case A: a .providers with no matching plugin-<word> symlink
$ run_assert
plugin-broken: plugin symlink missing
exit=1

$ # Case B: symlink present but the host emits no handshake
$ run_assert
plugin-broken: shared host did not serve (got )
exit=1
```

So the check is failure-capable: it cannot pass on a partially-broken welded set.

### Sweep — the legacy per-plugin path is gone (verbatim)

```
$ git grep -n "charly-plugins-linux" -- .
$ echo "exit=$?"
exit=1
```

`git grep` exits 1 and prints nothing on zero hits — no per-plugin tarball download/extract remains.

## Rulebook compliance

- **R0 — skills first:** `image`/`build` + `git-workflow` loaded.
- **R1 — RCA every anomaly:** the artifact-size root cause (N per-plugin binaries each re-linking the SDK/spec closure) is RCA'd in opencharly/sdk#256. Replacing the blanket `charly doctor` smoke with the explicit layout + per-plugin serve + charly-discovery assertions is operator-signed-off (above); the `charly doctor` host-capability check is owned by its named batch opencharly/charly#609.
- **R2 — finish the cutover:** ubuntu switches fully; no per-plugin fallback remains.
- **R3 — no duplication:** one host + symlinks, the same layout packagekit stages.
- **R4 — no workarounds:** the probe synchronises on the plugin's own handshake output via a FIFO + bounded `read -t 3` — no blind sleep, no retry.
- **R4a — fix the product first:** the SDK/packagekit product is fixed; this consumes it.
- **R5 — delete legacy completely:** the `charly-plugins-linux-*.tar.gz` download + per-plugin install are DELETED (sweep pasted verbatim); the stale "plugin tarballs" cleanup comment is corrected.
- **R6 — git safety:** isolated worktree, add-only commits, PR-only.
- **R7 — prove behavior:** the workflow ran live on this head, asserting the layout, every plugin served, and charly discovery; the assertion's negative path is pasted.
- **R8 — preserve emitted artifacts:** the emitted `deb`s + repo metadata were built, installed, and deployed live; the `.charly-lib/` staging is removed before the workspace-root Pages upload
- **R9 — binary equals source:** the host is fetched from the pinned `charly-lib` tag (`charly_lib_version` input, `v2026.256.1131`); the charly binary from the pinned charly release tag.
- **R10 — fresh proof:** run `34762342900` on head `94df911d` includes the disposable install-test (layout + per-plugin serve + discovery) and the Pages deploy.

## Change classification

- **Change class:** CI workflow (packaging cutover) — the distro package contents.
- **Verification gate:** the live workflow run on this head (build → install-test layout + per-plugin serve + charly discovery → Pages), plus the pasted negative path.
- **Attribution tier:** `fully tested and validated`.

*Assisted-by: opencode opencode-go/deepseek-v4.1-flash (fully tested and validated)*